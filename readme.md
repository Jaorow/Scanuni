to run first create an enviroment:
- create .env file in base directory
- fill .env file like so
    ```
    USERUPI = your upi
    PASSWORD = your password
    ```
then install requirements
```pip3 instal -r requirements.txt```